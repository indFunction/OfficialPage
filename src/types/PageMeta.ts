interface typeMeta {
    path?: string; // Dummy
    isPublish?: boolean; // Dummy
    thumbnail?: string;
    title?: string;
    description?: string;
    date?: number[];
    category?: string;
    keywords?: string[];
}

export default typeMeta;
